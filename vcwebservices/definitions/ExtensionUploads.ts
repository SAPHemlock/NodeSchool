import { ExtensionUpload } from "./ExtensionUpload";

/**
 * ExtensionUploads
 * @targetNSAlias `tns`
 * @targetNamespace `http://sma-promail/`
 */
export interface ExtensionUploads {
    /** ExtensionUpload[] */
    ExtensionUpload?: Array<ExtensionUpload>;
}
